---
phase: 05-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/games/snus-catcher/engine.ts
  - server/src/games/snus-catcher/engine.test.ts
  - shared/src/types.ts
  - server/src/games/registry.ts
  - client/src/games/snus-catcher/index.tsx
  - client/src/games/GameContainer.tsx
  - client/src/pages/Home.tsx
  - client/src/pages/Lobby.tsx
autonomous: true
requirements:
  - GAME-01
  - PLAT-01

must_haves:
  truths:
    - "SenusCatcherEngine implements GameEngine and emits ticks at 20 Hz when init is called"
    - "SenusCatcherEngine.destroy() clears the tick interval with no leak"
    - "TypeScript compiles clean across all three workspaces after the GameType union expansion"
    - "Snus Catcher appears as a selectable card on the Home page"
    - "Lobby game badge displays 'Snus Catcher' (not the raw string 'snus-catcher')"
    - "Game registry contains 'snus-catcher' entry pointing to SenusCatcherEngine"
  artifacts:
    - path: "server/src/games/snus-catcher/engine.ts"
      provides: "SenusCatcherEngine stub — GameEngine lifecycle + 20 Hz setInterval tick"
      exports: ["SenusCatcherEngine"]
    - path: "server/src/games/snus-catcher/engine.test.ts"
      provides: "RED→GREEN unit tests for engine lifecycle"
      contains: "init starts ticking, destroy clears interval"
    - path: "shared/src/types.ts"
      provides: "GameType union expanded to include 'snus-catcher'"
      contains: "'snusking' | 'snus-catcher'"
    - path: "server/src/games/registry.ts"
      provides: "gameRegistry entry for 'snus-catcher'"
      contains: "'snus-catcher': SenusCatcherEngine"
    - path: "client/src/games/snus-catcher/index.tsx"
      provides: "Placeholder Solid.js component (canvas div + loading text)"
      exports: ["SenusCatcherGame"]
    - path: "client/src/games/GameContainer.tsx"
      provides: "Show block routing 'snus-catcher' gameType to SenusCatcherGame"
      contains: "gameType() === 'snus-catcher'"
    - path: "client/src/pages/Home.tsx"
      provides: "Snus Catcher entry in GAMES array"
      contains: "id: 'snus-catcher'"
    - path: "client/src/pages/Lobby.tsx"
      provides: "Game badge label for 'snus-catcher' displays 'Snus Catcher'"
      contains: "'snus-catcher' ? 'Snus Catcher'"
  key_links:
    - from: "server/src/games/registry.ts"
      to: "server/src/games/snus-catcher/engine.ts"
      via: "import SenusCatcherEngine"
      pattern: "SenusCatcherEngine"
    - from: "client/src/games/GameContainer.tsx"
      to: "client/src/games/snus-catcher/index.tsx"
      via: "import SenusCatcherGame"
      pattern: "SenusCatcherGame"
    - from: "client/src/pages/Home.tsx"
      to: "shared/src/types.ts"
      via: "GameType literal must match 'snus-catcher'"
      pattern: "id: 'snus-catcher'"
---

<objective>
Register Snus Catcher as a second game type on the platform. Create a real-time stub engine
that ticks at 20 Hz and wire all four TypeScript propagation points atomically.

Purpose: Establishes the structural foundation so Phase 6 can add gameplay logic without
touching registration or routing.

Output:
- SenusCatcherEngine stub (server) with unit tests
- GameType union expanded in shared/src/types.ts
- All four propagation points updated: shared types, registry, GameContainer, Home.tsx
- Lobby.tsx badge label polished to "Snus Catcher"
- Placeholder SenusCatcherGame client component
</objective>

<execution_context>
@C:/Users/addes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/addes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-foundation/05-RESEARCH.md
@.planning/phases/05-foundation/05-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From shared/src/types.ts (line 30):
```typescript
export type GameType = 'snusking';
// Phase 5 change → 'snusking' | 'snus-catcher'
```

From server/src/games/registry.ts:
```typescript
export interface GameEngine {
    init(roomId: string, players: PlayerInfo[], onStateUpdate: (state: unknown) => void): void;
    handleEvent(playerId: string, action: GameAction): void;
    getState(): unknown;
    destroy(): void;
}

export const gameRegistry: Record<string, new () => GameEngine> = {
    'snusking': SnuskingEngine,
    // Phase 5: add 'snus-catcher': SenusCatcherEngine
};
```

From client/src/games/GameContainer.tsx (existing Show pattern):
```typescript
// Existing Snusking Show block (lines 36–44):
<Show when={gameType() === 'snusking' ? gameState() : null}>
    {(state) => (
        <SnuskingGame
            state={state() as SnuskingProjectedState}
            roomCode={props.roomCode}
            onAction={(action) => socket.emit('game:action', { roomCode: props.roomCode, action: action as GameAction })}
        />
    )}
</Show>

// Phase 5 addition DOES NOT gate on gameState() — placeholder has no state prop:
<Show when={gameType() === 'snus-catcher'}>
    <SenusCatcherGame roomCode={props.roomCode} />
</Show>
```

From client/src/pages/Home.tsx (GAMES array, line 11):
```typescript
const GAMES: Game[] = [
    {
        id: 'snusking',
        name: 'Snusking',
        tagline: 'Collect. Spend. Dominate.',
        description: '...',
        badges: ['2–4 Players', 'Strategy', 'Card Game'],
    },
    // Phase 5: add Snus Catcher entry
];
```

From client/src/pages/Lobby.tsx (line 104):
```typescript
// Current:
{r().gameType === 'snusking' ? '🫙 Snusking' : r().gameType}
// Phase 5 change:
{r().gameType === 'snusking' ? '🫙 Snusking' : r().gameType === 'snus-catcher' ? 'Snus Catcher' : r().gameType}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SenusCatcherEngine stub + RED test stubs</name>
  <files>server/src/games/snus-catcher/engine.ts, server/src/games/snus-catcher/engine.test.ts</files>
  <behavior>
    - Test: init() starts emitting state at ~20 Hz (use fake timers; advance 100ms, expect onStateUpdate called at least once)
    - Test: destroy() clears the interval (advance timers after destroy, expect onStateUpdate NOT called again)
    - Test: calling init() twice does not accumulate two intervals (only one set of ticks fires)
    - Test: getState() returns an object with a tickCount property
  </behavior>
  <action>
    Create directory `server/src/games/snus-catcher/` if it does not exist.

    Write `server/src/games/snus-catcher/engine.ts`:
    ```typescript
    import type { GameEngine } from '../registry';
    import type { PlayerInfo, GameAction } from '@slutsnus/shared';

    const TICK_MS = 50; // 20 Hz — locked per STATE.md

    export class SenusCatcherEngine implements GameEngine {
      private tickInterval?: ReturnType<typeof setInterval>;
      private onStateUpdate: (state: unknown) => void = () => {};
      private tickCount = 0;

      init(_roomId: string, _players: PlayerInfo[], onStateUpdate: (state: unknown) => void): void {
        // Guard against double-init accumulation (Pitfall 2 from research)
        if (this.tickInterval) {
          clearInterval(this.tickInterval);
        }
        this.onStateUpdate = onStateUpdate;
        this.tickCount = 0;
        this.tickInterval = setInterval(() => this.tick(), TICK_MS);
      }

      private tick(): void {
        this.tickCount++;
        this.onStateUpdate({ tickCount: this.tickCount, status: 'playing' });
      }

      handleEvent(_playerId: string, _action: GameAction): void {
        // Phase 5: no-op — Phase 6 implements bar movement and collision
      }

      getState(): unknown {
        return { tickCount: this.tickCount, status: 'playing' };
      }

      destroy(): void {
        if (this.tickInterval) {
          clearInterval(this.tickInterval);
          this.tickInterval = undefined;
        }
      }
    }
    ```

    Write `server/src/games/snus-catcher/engine.test.ts`:
    Use Vitest's `vi.useFakeTimers()` and `vi.advanceTimersByTime()` — NOT real timers.
    Import `SenusCatcherEngine` from `./engine`.
    Write four tests matching the behavior block above.
    Run RED first: confirm tests fail if you comment out the implementation, then restore and confirm GREEN.
  </action>
  <verify>
    <automated>cd C:/Users/addes/Git/Slutsnus && npm run test --workspace=server -- --reporter=verbose 2>&1 | grep -E "(snus-catcher|PASS|FAIL|✓|✗)"</automated>
  </verify>
  <done>engine.test.ts has 4 passing tests; engine.ts exports SenusCatcherEngine implementing GameEngine; npm run test --workspace=server is green</done>
</task>

<task type="auto">
  <name>Task 2: Atomic GameType propagation — all four files + client placeholder</name>
  <files>shared/src/types.ts, server/src/games/registry.ts, client/src/games/snus-catcher/index.tsx, client/src/games/GameContainer.tsx, client/src/pages/Home.tsx, client/src/pages/Lobby.tsx</files>
  <action>
    CRITICAL: All six files below must be written and committed together. TypeScript will produce
    cascading errors if any file lags. After all edits, verify with tsc --noEmit on all three workspaces.

    1. `shared/src/types.ts` — expand GameType union (line 30):
       Change: `export type GameType = 'snusking';`
       To:     `export type GameType = 'snusking' | 'snus-catcher';`

    2. `server/src/games/registry.ts` — add registry entry:
       Add import: `import { SenusCatcherEngine } from './snus-catcher/engine';`
       Add to gameRegistry: `'snus-catcher': SenusCatcherEngine,`

    3. Create `client/src/games/snus-catcher/index.tsx` — placeholder component:
       ```typescript
       interface SenusCatcherGameProps {
           roomCode: string;
       }

       export function SenusCatcherGame(_props: SenusCatcherGameProps) {
           return (
               <div style={{ display: 'flex', 'align-items': 'center', 'justify-content': 'center', height: '100vh', color: '#e6edf3' }}>
                   <p>Snus Catcher — laddas...</p>
               </div>
           );
       }
       ```
       No canvas needed in Phase 5 — Phase 6 adds the real game canvas.

    4. `client/src/games/GameContainer.tsx` — add Show block for 'snus-catcher':
       Import `SenusCatcherGame`: add to imports at top.
       Add after the existing Snusking Show block, inside the `<div class="game-wrapper">`:
       ```typescript
       <Show when={gameType() === 'snus-catcher'}>
           <SenusCatcherGame roomCode={props.roomCode} />
       </Show>
       ```
       Do NOT touch the existing Snusking Show block or the gameState signal type — defer
       signal type widening to Phase 6 per research recommendation.

    5. `client/src/pages/Home.tsx` — add Snus Catcher to GAMES array:
       Append to GAMES array after the Snusking entry:
       ```typescript
       {
           id: 'snus-catcher',
           name: 'Snus Catcher',
           tagline: 'Catch or die.',
           description: 'Catch fresh snus pouches falling from above. Dodge the spent ones. 1v1 arcade racing.',
           badges: ['2 Players', 'Arcade', 'Real-time'],
       },
       ```

    6. `client/src/pages/Lobby.tsx` — polish game badge label (line 104):
       Change:
         `{r().gameType === 'snusking' ? '🫙 Snusking' : r().gameType}`
       To:
         `{r().gameType === 'snusking' ? '🫙 Snusking' : r().gameType === 'snus-catcher' ? 'Snus Catcher' : r().gameType}`

    After all edits, run TypeScript check across all three workspaces to confirm clean compilation.
  </action>
  <verify>
    <automated>cd C:/Users/addes/Git/Slutsnus && npx tsc --noEmit --project shared/tsconfig.json && npx tsc --noEmit --project server/tsconfig.json && npx tsc --noEmit --project client/tsconfig.json && echo "All workspaces clean"</automated>
  </verify>
  <done>All three tsc --noEmit checks pass with zero errors; 'snus-catcher' appears in GameType union, gameRegistry, GAMES array, GameContainer Show block; Lobby badge label resolves to 'Snus Catcher'</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run test --workspace=server` — all tests green (4 new engine lifecycle tests)
2. `npx tsc --noEmit --project shared/tsconfig.json` — clean
3. `npx tsc --noEmit --project server/tsconfig.json` — clean
4. `npx tsc --noEmit --project client/tsconfig.json` — clean
5. grep confirms: `shared/src/types.ts` contains `'snus-catcher'`, `server/src/games/registry.ts` contains `SenusCatcherEngine`, `client/src/pages/Home.tsx` contains `id: 'snus-catcher'`
</verification>

<success_criteria>
- SenusCatcherEngine unit tests pass (init/tick/destroy/double-init)
- GameType union in shared/src/types.ts is `'snusking' | 'snus-catcher'`
- gameRegistry has `'snus-catcher': SenusCatcherEngine` entry
- Client builds without TypeScript errors
- Home page has a second game card entry (Snus Catcher)
- GameContainer routes 'snus-catcher' gameType to SenusCatcherGame placeholder
</success_criteria>

<output>
After completion, create `.planning/phases/05-foundation/05-01-SUMMARY.md`
</output>
