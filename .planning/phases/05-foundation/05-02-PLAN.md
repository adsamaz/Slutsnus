---
phase: 05-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified: []
autonomous: false
requirements:
  - GAME-01
  - PLAT-01

must_haves:
  truths:
    - "Snus Catcher game card is visible and selectable on the Home page"
    - "Player can create a Snus Catcher room from the lobby and a second player can join it"
    - "Server emits game:state tick events to both sockets once the room starts"
    - "Lobby game badge shows 'Snus Catcher' (not raw 'snus-catcher' string)"
  artifacts:
    - path: "client/src/games/snus-catcher/index.tsx"
      provides: "Rendered placeholder (visible in browser at /game/:code)"
    - path: "server/src/games/snus-catcher/engine.ts"
      provides: "Running tick loop — observable in browser console"
  key_links:
    - from: "client (browser)"
      to: "server SenusCatcherEngine"
      via: "room:start socket event triggers engine.init() → setInterval → game:state emissions"
      pattern: "game:state"
---

<objective>
Human verification that the Phase 5 wiring works end-to-end: game card selectable in lobby,
room creation/join flow functional, and server ticks reaching both connected sockets.

Purpose: Confirms the structural registration is correct before Phase 6 adds gameplay.

Output: Approved checkpoint confirming GAME-01 and PLAT-01 are satisfied in the running app.
</objective>

<execution_context>
@C:/Users/addes/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/addes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-foundation/05-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 05-01 wired Snus Catcher across all four TypeScript propagation points:
    - shared/src/types.ts: GameType union expanded to include 'snus-catcher'
    - server/src/games/registry.ts: SenusCatcherEngine registered
    - client/src/pages/Home.tsx: Snus Catcher game card added to GAMES array
    - client/src/games/GameContainer.tsx: Show block routes 'snus-catcher' to placeholder
    - client/src/pages/Lobby.tsx: badge label shows 'Snus Catcher'
    - server/src/games/snus-catcher/engine.ts: 20 Hz setInterval tick engine stub
  </what-built>
  <how-to-verify>
    Start the dev server if not already running (npm run dev or equivalent from workspace root).

    Step 1 — Home page game card:
    Open http://localhost:5173 (or the dev port) in a browser.
    Confirm a "Snus Catcher" card appears below the Snusking card with tagline "Catch or die."
    and badges: 2 Players, Arcade, Real-time.

    Step 2 — Room creation:
    Log in (if not already). Click the Snus Catcher card to select it.
    Click "Create Room". You should be redirected to /lobby/:code.
    Confirm the lobby badge shows "Snus Catcher" (not the raw string "snus-catcher").

    Step 3 — Second player joins:
    Open a second browser tab or incognito window.
    Log in as a different user.
    Enter the room code from Step 2 and click "Join".
    Both tabs should show the same lobby with two players listed.

    Step 4 — Server ticks:
    In the first tab, open the browser DevTools console.
    The host clicks "Starta spel" (both players must be ready first — second player clicks "Redo").
    After starting, watch the console for `game:state` events. You should see tick events
    arriving approximately every 50 ms (20 Hz). The placeholder "Snus Catcher — laddas..."
    screen should appear on both tabs.

    Expected outcomes:
    - Snus Catcher card visible and selectable on Home page
    - Lobby badge: "Snus Catcher" (correct label)
    - Both players land on the placeholder screen after start
    - Console shows repeated game:state events with { tickCount, status: 'playing' }
  </how-to-verify>
  <resume-signal>Type "approved" if all four steps pass, or describe any issues observed</resume-signal>
</task>

</tasks>

<verification>
Human sign-off on all four verification steps above constitutes phase completion.
Automated checks already run in Plan 05-01 (tests green, tsc clean).
</verification>

<success_criteria>
- PLAT-01: Snus Catcher card visible and selectable on Home page
- GAME-01: Player can create a Snus Catcher room and a second player can join it
- Server emits game:state ticks to both sockets after room starts
- TypeScript already verified clean in Plan 05-01
</success_criteria>

<output>
After checkpoint is approved, create `.planning/phases/05-foundation/05-02-SUMMARY.md`
</output>
